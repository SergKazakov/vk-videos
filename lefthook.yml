pre-commit:
  parallel: true
  commands:
    dedupe:
      glob: "package.json"
      run: bun dedupe --check
    eslint:
      glob: "*.{mjs,mts}"
      run: bunx eslint --fix --cache {staged_files} && git add {staged_files}
    tsc:
      glob: "*.mts"
      run: bunx tsc
