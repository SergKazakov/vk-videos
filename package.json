{
  "private": true,
  "scripts": {
    "build": "bun build ./src/index.mts --compile --outfile=cli",
    "start": "node --experimental-strip-types --env-file=.env ./src/index.mts"
  },
  "prettier": {
    "arrowParens": "avoid",
    "semi": false
  },
  "dependencies": {
    "axios": "^1.11.0",
    "mongodb": "^6.19.0",
    "puppeteer-core": "^24.17.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  }
}
